import { StyleSheet } from "react-native";
import { useGlobals } from "./provider";


export default function Catalog() {
    const { productList } = useGlobals();
    return (
        <>
        </>
    );
}

const styles = StyleSheet.create({

})